# Everything under data/raw/ is derived: the Geofabrik download itself, the
# pyosmium prefilters, and the boundary/line caches built from them. All of it
# is regenerated by `python -c "import ie_distribution_osm as m; m.prepare()"`
# plus the analysis scripts, and national_lines.gpkg alone is 115 MB - over
# GitHub's 100 MB per-file limit.
data/raw/
__pycache__/
*.pyc
